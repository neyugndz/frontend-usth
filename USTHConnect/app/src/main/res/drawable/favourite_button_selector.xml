<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This state is applied when the item is marked as favourite -->
    <item android:state_selected="true" android:drawable="@drawable/filled_star" />
    <!-- Default state (not selected, not a favourite) -->
    <item android:drawable="@drawable/favorite_course" />
</selector>
